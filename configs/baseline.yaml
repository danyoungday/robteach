robots: "Panda"
env_cls_path: "baseline_env.py"

env_kwargs:
  has_renderer: false
  has_offscreen_renderer: false
  use_object_obs: true
  use_camera_obs: false
  reward_shaping: true
  control_freq: 20
  horizon: 500
  hard_reset: false

n_envs: "auto"
device: "cpu"

ppo_kwargs:
  learning_rate: 3.0e-4
  n_steps: 128
  batch_size: 256
  n_epochs: 10
  gamma: 0.99
  gae_lambda: 0.95
  clip_range: 0.2
  ent_coef: 0.001
  vf_coef: 0.5
  max_grad_norm: 0.5
  verbose: 1
  policy_kwargs:
    log_std_init: -2
    ortho_init: false
    activation_fn: "ReLU"
    net_arch:
      pi: [256, 256]
      vf: [256, 256]

total_timesteps: 5_000_000
save_dir: "results/baseline"
checkpoint_freq: 100_000
eval_freq: 100_000

wandb:
  project: "robosuite"   # user fills this in
  entity: null  # optional, uses default entity if null
